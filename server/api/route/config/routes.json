{
  "routes": [
    {
      "method": "GET",
      "path": "/route",
      "handler": "route.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/route",
      "handler": "route.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/route",
      "handler": "route.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
